export enum tagTypes {
  account = "account",
  booking = "booking",
  cart = "cart",
  user = "user",
  order = "order",
  withdraw = "withdraw",
}
export const tagTypesList = Object.values(tagTypes);
